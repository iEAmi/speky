package com.github.speky.postgres.executor

import io.kotest.core.spec.style.FunSpec

internal class PgSpecificationExecutorTest : FunSpec({
  test("create PgSpecificationExecutor instance") {
//    val executor = PgSpecificationExecutor(TODO(), TODO())
  }
})
