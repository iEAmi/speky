package com.github.speky.compiler.pg

import com.github.speky.compiler.Compiler
import com.github.speky.core.specification.Selected

//internal class SelectedCompiler<T>(private val compiler: PgSpecificationCompiler<T>) :
//  Compiler<Selected<T>, String> {
//  override fun compile(input: Selected<T>): String = when (input) {
//    is Selected.All  -> TODO()
//    is Selected.Some -> TODO()
//  }
//}
